[project]
name = "webbed_duck"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.0.0",
    "pyarrow>=16.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
