# Environment Setup Lessons

- Capture Python and DuckDB versions in logs to ensure reproducible cache behaviour.
- Prefer lightweight, per-feature virtual environments to keep optional dependencies isolated.
- Document required system libraries immediately after discovery to avoid repeated setup churn.
